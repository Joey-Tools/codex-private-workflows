version = 1
id = "weekly-session-retrospective"
kind = "cron"
name = "Weekly Session Retrospective"
prompt = """
Run a read-only weekly retrospective over Joey's Codex session activity.

Use /Users/hoteng/Program/GitHub/Joey-Tools/codex-workspace as the stable workspace cwd. Treat all local and remote Codex history as read-only. Do not modify ~/.codex, remote hosts, Apple Notes, or raw rollout files.

Evidence scope must match $remote-host-context's default host policy: local machine, miku-bot-dev, and hoteng-srv-01. Start with remote-host-context preflight across those hosts. For remote evidence, use the remote_codex_probe.py bounded flow: preflight, session-meta for bounded dates, fetch-rollout for verified rollout paths, and rollout-summary when a rollout is too large to copy. Do not use unbounded remote rg/jq/ssh searches. If a host is unreachable, stale, missing ~/.codex, or gated by auth, report that exact coverage gap.

Use the codex-session-retrospective workflow from the codex-workflow-hygiene canonical mirror under this workspace. The helper path is .codex-local/daily-skill-friction/repos/codex-workflow-hygiene/skills/codex-session-retrospective/scripts/session_retrospective.py. If that mirror is missing, dirty, stale, or missing the helper, report blocked rather than cloning or falling back to an unrelated repo.

Scan the last 7 days across all default hosts. Use a bounded map-reduce workflow: helper-generated host/source and shard manifests first; maximum practical subagent concurrency for extractor-redactor shards; aggregation subagents over redacted turn summaries; high-signal episode reviewers for noisy, slow, confusing, risky, or repeatedly corrected episodes; and a cross-episode synthesizer for durable guidance candidates. Later stages must not read raw transcript text.

Write task-local artifacts under .codex-local/session-retrospective/runs/<YYYYMMDD>/weekly/. The history repository is Joey-Tools/codex-session-retrospective-history. Use scripts/codex_workspace.py status --repo codex-session-retrospective-history --strict before preparing a write worktree. If ready, use scripts/codex_workspace.py prepare-run --repo codex-session-retrospective-history --date <YYYYMMDD> --topic weekly-session-retrospective and commit only redacted reports, episode summaries, turn flags, trend JSON, and schema-valid artifacts. If the history repo mirror is unavailable, keep the local cache and report `history commit blocked`.

Do not commit raw rollout files, full prompts, internal URLs, secrets, customer data, proprietary code snippets, or unredacted tool output. Report the scanned window, host coverage, subagent waves used, history commit status, repeated friction, prompt improvements, AGENTS/skill/automation suggestions, verification gaps, and safety/privacy flags.
"""
status = "ACTIVE"
rrule = "FREQ=WEEKLY;BYDAY=SUN;BYHOUR=4;BYMINUTE=10"
model = "gpt-5.5"
reasoning_effort = "xhigh"
execution_environment = "worktree"
cwds = ["/Users/hoteng/Program/GitHub/Joey-Tools/codex-workspace"]
created_at = 1779408000000
updated_at = 1779408000000
