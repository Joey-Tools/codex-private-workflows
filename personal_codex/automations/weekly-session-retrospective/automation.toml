# Reference-only release template. Its presence in this repository or a release
# package is not evidence that a live Codex automation is registered.
# Cutover contract:
# - Authoritative live path: ~/.codex/automations/weekly-session-retrospective/automation.toml
# - If that exact path is absent, install this ID as an exact first registration.
# - If that exact path is a verified regular TOML file whose id is exactly
#   "weekly-session-retrospective", perform a verified in-place update of it.
# - A path/type/parse/ID mismatch blocks cutover; never infer registration from
#   this template or create a second automation as a fallback.
# The timestamps below are release-template metadata, not live-registration evidence.
version = 1
id = "weekly-session-retrospective"
kind = "cron"
name = "Weekly Session Retrospective"
prompt = """
Run the installed Session Retrospective v2 Weekly workflow over Joey's Codex session activity.

Use /Users/hoteng/Program/GitHub/Joey-Tools/codex-workspace as the stable workspace cwd. Read /Users/hoteng/.codex/skills/codex-session-retrospective/SKILL.md and use exactly /Users/hoteng/.codex/skills/codex-session-retrospective/scripts/session_retrospective_v2.py. Do not use the v1 entry point, a source-checkout fallback, or a different coordinator path. If the installed v2 CLI or its required production configuration is absent or fails its machine-readable help/doctor contract, report blocked without changing retrospective state.

Follow the installed skill's production Weekly contract and coordinator state machine. Use the exact configured production host set and the native status-authenticated `session-shards`/`accept-source` bridge; do not silently omit a host, suppress a production source, substitute bare SSH or another transport, or allow partial Weekly coverage. Keep raw rollout evidence read-only and transient. Later stages may consume only validated redacted structures.

Use one exact closed seven-day window and the authoritative production identity, configuration, history, and cursor bindings selected by the v2 engine. Parse every coordinator response as one `cli_result_v2` JSON object. Advance, export, finalize, publish, and move production state only through the installed v2 workflow and only after its authenticated coverage, privacy, retained-output, history, and provider gates succeed. An unresolved source lease, coverage gap, identity/configuration mismatch, or publication failure must not advance production state.

Do not commit raw rollout files, full prompts, internal URLs, secrets, customer data, proprietary code snippets, or unredacted tool output. Report the exact window, host coverage and gaps, configuration root, subagent waves, publication/state outcome, repeated friction, prompt improvements, AGENTS/skill/automation suggestions, verification gaps, and safety/privacy flags.
"""
status = "ACTIVE"
rrule = "FREQ=WEEKLY;BYDAY=SUN;BYHOUR=4;BYMINUTE=10"
model = "gpt-5.6-terra"
reasoning_effort = "high"
execution_environment = "worktree"
cwds = ["/Users/hoteng/Program/GitHub/Joey-Tools/codex-workspace"]
created_at = 1779408000000
updated_at = 1782558779000
