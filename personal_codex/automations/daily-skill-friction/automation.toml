version = 1
id = "daily-skill-friction"
kind = "cron"
name = "Daily Skill Friction"
prompt = "Run inside the dedicated worktree provisioned for this automation. If the runtime did not provision one, create a disposable task-scoped worktree or branch named `codex/automation-daily-skill-friction-<YYYYMMDD>` before doing anything else. Keep all task artifacts under `.codex-tmp/daily-skill-friction-<YYYYMMDD>/` inside that worktree. Clean them up before finishing when safe, then perform exactly one deterministic post-cleanup check on the task artifact directory and its parent and report either `Cleanup verified:` with the removed paths or `Leftover:` with the remaining paths; do not claim cleanup succeeded if that final check disagrees. Use the automation's configured model and reasoning effort unless the runtime refuses them; if it must fall back, report the exact fallback. Record the actual audit start time in the final report. When reading this automation's memory, do not dump the whole file or a fixed 200-line head; use a structured parser or narrowly bounded extraction to find the latest completed-run End timestamp, and widen only if that parse fails. Before scanning sessions, read `~/.codex/automations/daily-skill-friction/memory.md` and identify the most recent previously completed run entry whose recorded end timestamp is earlier than the current audit start. Use that prior run end timestamp as the lower bound, then scan `~/.codex/sessions` for repeated or newly emerging skill friction, workflow friction, command-shape friction, approval/auth friction, and review-lane friction across all rollouts since that timestamp rather than only the last 24 hours. In the final report, state the exact lower bound you used and whether it came from `memory.md` or a fallback window. If no usable prior completed run entry can be recovered from `memory.md`, explicitly report that gap and fall back to scanning the last 24 hours. Use [$codex-session-mining](/Users/hoteng/.codex/skills/codex-session-mining/SKILL.md) to mine the sessions and [$joey-skill-authoring](/Users/hoteng/.codex/skills/joey-skill-authoring/SKILL.md) to decide whether any fix belongs in a personal skill, `~/.codex/AGENTS.md`, an existing repo-local skill elsewhere, or a different mechanism such as Codex Automations or Rules. When reconstructing the real user task from rollouts, ignore injected wrapper content such as `<skill>` blocks, repo or personal `AGENTS.md` scaffolding, automation boilerplate, system or developer instruction wrappers, remark-directive templates, and synthetic subagent or external-review prompts unless the user explicitly typed them; base friction judgments on the user's substantive requests and the assistant's resulting behavior. When excluding the current automation, exclude only the automation wrapper turn(s), generated task-artifact chatter, and obvious self-referential bookkeeping; do not discard later human follow-up turns in the same thread solely because the rollout began with the automation wrapper. Do not focus only on skill trigger misses; also audit whether the assistant used an awkward command shape, missed an obvious guardrail, got stuck in review-lane or session-shape handling, or otherwise needed the user to manually repair the workflow. Classify each candidate as `repeated`, `novel but not yet repeated`, or `no issue`. When a change is warranted, route the write to the Joey-Tools canonical repository before touching the legacy aggregate mirror. Use this ownership map: `Joey-Tools/codex-toolbox` for public personal Codex base packaging, public AGENTS/mirror release content, and shared public helper packaging; `Joey-Tools/codex-personal-sync` for sync manifests, installer, rollback, release-install, and scheduler logic; `Joey-Tools/codex-project-journal` for the project-journal skill, journal templates, hook/index tooling, and tests; `Joey-Tools/codex-review-workflows` for review orchestration, change delivery, PR readiness, readonly git shim, external review, and Copilot review workflows; `Joey-Tools/codex-workflow-hygiene` for skill authoring, session mining, rules hygiene, and general workflow-friction guardrails; `Joey-Tools/codex-debug-triage` for generic bug/build/log/archive triage; `Joey-Tools/codex-apple-notes-toolkit` for public-safe generic Apple Notes tooling and DB guardrails; `Joey-Tools/codex-rollout-backup` for rollout mirror, snapshot, cloud-unpin, launchd, and systemd backup logic; `Joey-Tools/codex-private-workflows` for private overlays, private AGENTS/reviewer config, private automations including this automation, Apple Notes Work Report semantics, Cisco/GHE/Jira helpers, and Joey remote-host aliases. Use `/Users/hoteng/Program/GitHub/Joey-Tools/codex-workspace` as the stable local canonical workspace. Choose the workspace repo name by taking the final repository component from the ownership map entry, for example `Joey-Tools/codex-review-workflows` maps to `codex-review-workflows`. A host-level scheduler should run `python3 scripts/codex_workspace.py prefetch --stamp daily-skill-friction` from the workspace about 20 minutes before this 03:10 automation run, normally at 02:50 local time; that external prefetch is the only place this workflow should rely on GitHub network access. To create the target repo worktree, first run `python3 scripts/codex_workspace.py status --repo <workspace-repo-name> --strict`, then use `python3 scripts/codex_workspace.py prepare-run --repo <workspace-repo-name> --date <YYYYMMDD> --topic <topic> --offline --fresh-stamp daily-skill-friction --fresh-within-minutes 60`; make the canonical change inside the returned worktree. If the freshness stamp is missing, stale, failed, or does not match the selected mirror, report blocked as `external mirror prefetch missing/stale` instead of attempting a GitHub fetch inside this automation. Treat `.codex-local/daily-skill-friction/repos/<repo>` checkouts as read-only clean configured default-branch mirrors only (`master` in the current workspace manifest). Do not edit files, create branches, commit, stash, or run repo-changing repair commands inside a mirror clone. All target-repo modifications, validation, commits, pushes, and PR creation must happen inside the returned run worktree. Do not clone GitHub repositories during automation runs. If `prepare-run` reports that the selected target mirror is missing, dirty, stale, not freshly prefetched, or on the wrong branch, report blocked instead of falling back to `JoeyTeng/Codex-maintenance`. You may run `python3 scripts/codex_workspace.py status` for a full mirror audit, but unrelated mirror issues must not block a target-specific change. If push or PR creation is unavailable, leave a signed local commit on the canonical repo branch and report the exact worktree, branch, and commit. Validate and commit the canonical repo change first. Only after the canonical change is validated and committed or PR-ready, sync the matching legacy aggregate mirror in `JoeyTeng/Codex-maintenance` when that mirror still exists, and record the canonical repository plus commit or PR that the aggregate mirror reflects. If canonical update and aggregate sync cannot both be completed, prefer the canonical update and report the aggregate sync as pending. Change personal skills or `~/.codex/AGENTS.md` when there is clear repeated evidence or when a single new high-signal issue is concrete enough to justify a narrow guardrail; do not create or edit unrelated repo-local skills from this task. Validate any touched skill with `uv run --isolated --with pyyaml ... quick_validate.py` when applicable, run the directly relevant tests for the canonical repository, run `git diff --check`, and create signed commits only when there is an actual validated repo diff to commit. If there is no validated repo diff after the audit, do not create a commit. When no rule change is warranted, still report either `Novel but not yet repeated:` or `No change warranted.` plus the decisive evidence and the watchpoint you would monitor next. Lead with the decisive evidence or findings rather than long narration."
status = "ACTIVE"
rrule = "FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR,SA,SU;BYHOUR=3;BYMINUTE=10"
model = "gpt-5.5"
reasoning_effort = "xhigh"
execution_environment = "worktree"
cwds = ["/Users/hoteng/Program/GitHub/Joey-Tools/codex-workspace"]
created_at = 1774100606412
updated_at = 1779396009953
