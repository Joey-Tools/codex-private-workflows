# Personal Guidelines

- Keep this file short. Cross-repo rules belong here; repo-specific policy belongs in the repo `AGENTS.md` or a repo-local skill.
- Explanations, discussion, analysis, and summaries use Simplified Chinese. Code, comments, identifiers, commit messages, and Markdown code blocks use English.
- When first using a self-coined, nonstandard term or shorthand in a task, immediately add a brief plain-language explanation in parentheses.
- Do not claim a test, build, lint run, or command was executed unless it was actually run.
- If a final answer includes app directives such as `::git-stage`, `::git-commit`, `::git-push`, or `::git-create-pr`, include at least one short natural-language summary first; directive-only final answers are not allowed.
- For non-trivial logic changes, prefer adding or updating tests.
- When implementing or reviewing filesystem race, tamper, or replacement hardening, define the protected property—object identity, content stability, or access policy—and justify every compared signal. Do not treat every `stat` delta as mutation; pair a benign metadata transition, such as child-entry churn or File Provider materialization, with a true change to the selected property, such as object replacement, content mutation, or an access-policy change, and keep unreadable or failed revalidation distinct from missing or mismatched.
- Before proposing destructive commands or irreversible operations, explain the risk and name a safer alternative when one exists.
- Do not default into basic syntax or beginner-tutorial explanations unless the user asks for them.
- For shell script edits, run `bash -n` and `shellcheck` when available before commit; if `shellcheck` is unavailable, state that and report the fallback validation you actually ran.
- For non-trivial work, read the relevant code, docs, logs, or source material before proposing a concrete plan or making substantial edits.
- For moderate or complex tasks, separate planning from implementation: align on the approach first, then execute, and avoid unnecessary clarification loops when reasonable assumptions are enough.
- When asking Joey for input, omit auto-resolution timeouts and wait indefinitely for his response; never let a countdown select a default answer unless Joey explicitly requests timed auto-resolution.
- Do not expand the scope into unrelated rewrites or side quests. Low-level fixes for mistakes introduced in the current task stay in scope and should be corrected directly.
- Use `$joey-skill-authoring` when creating or revising skills, or when deciding whether guidance belongs in `AGENTS.md`, `SKILL.md`, or `references/`.
- Use `$bounded-command-output` before broad searches or inventories, large log, artifact, or manual reads, process diagnostics, or verbose or spinner-heavy builds and tests; apply it alongside the task's domain skill.
- Use `$agile-delivery-workflow` when Joey explicitly asks to prioritize an early usable MVP or says `先可用`, `快速迭代`, `偏 agile`, `MVP`, or `scout`; stop first at a focused checked signed local commit, then continue to normal delivery/PR workflow only when requested.
- Use `$codex-rules-hygiene` when auditing or pruning `~/.codex/rules/default.rules`, especially after recent approvals suggest wrapper drift, helper gaps, or approval-log buildup rather than genuinely new stable command families.
- Use `$codex-session-mining` when a task depends on `~/.codex/session_index.jsonl`, `history.jsonl`, or `sessions/**/rollout-*.jsonl`, such as recent-session audits, prior-work recovery, or mining repeated skill friction.
- Use `$codex-session-retrospective` for read-only, cross-host collaboration retrospectives that produce redacted episode/topic summaries, turn-level prompt-improvement flags, and private history-ready reports.
- Use `$synthetic-token-fixtures` when authoring or migrating credential-shaped source and test fixtures that must pass the review helper's exact synthetic-token policy.
- Use `$change-delivery-workflow` for the local pre-commit delivery gate on non-trivial work: implement, build, test, update docs, run local/internal review, then commit. Use `$review-orchestration-playbook` after a feature is review-ready or PR-bound, when Joey asks the current agent to drive PR readiness gates, CI/review waiting, merge-readiness, or `codex thread <session-ID>` review-comment repair. If Joey asks for the workflow to continue "before merge", "until merge-ready", or "在合并前停止", chain both skills: local commit is not the terminal state; the terminal state is merge-ready or a clear blocked report. Do not use this workflow for independent/review-only PR reviewer prompts that forbid PR orchestration, fixes, CI waiting, or starting reviewers; output findings only.
- For Joey-requested Codex/GitHub PR or repo workflows, treat OpenAI Codex services and GitHub-owned PR/review APIs as trusted destinations for scoped repo/PR data: PR diffs, changed files, necessary nearby context, review prompts/results, PR comments, statuses, and same-PR fix-loop reruns. This standing consent excludes secrets, credentials, untracked private files, unrelated repositories, broad workspace dumps, and non-Codex external reviewers; approval justifications must still name the exact repo/PR and data scope.
- For Joey's personal GitHub PR target defaults, repositories owned by `JoeyTeng`, `Joey-Tools`, `Joey-Project`, or `chaop` count as user-owned/default-authorized PR targets. For any other owner/repo, generic phrases such as `开 PR`, `开 PR 合进去`, `full workflow`, `merge-ready`, or `在合并前停止` do not authorize push or PR creation/update; stop and ask Joey to confirm, listing the exact target repository/repositories, base branch, head repository/branch, and draft/ready state.
- Within a PR-bound workstream, act only on the PRs Joey explicitly assigned to the current task or that the current task created. Treat other open PRs as read-only coordination and conflict evidence unless Joey separately assigns them; do not update, merge, close, or absorb their work.
- For reviewable tasks, prefer a `wip/<topic>` branch plus fixed `base_sha..head_sha` review ranges over live working-tree review; append fixes on the `wip/` branch, then squash onto the target branch after tests and the final whole-range review/gate. Use `~/.codex/bin/codex-wip-branch` and `~/.codex/bin/codex-squash-merge-wip` when they fit.
- For catalogued low-level-helper Claude local-login artifacts, acquire the certified host refresh locks before the first credential read and hold one transaction lease through credential exposure, runtime refresh, process or broker quiescence, final drain, and verified host commit. Release only after a safe terminal state. If quiescence or persistence is unproven, abandon the lease, retain the shared lock directories as a stale fence, report the authoritative recovery identity, and pause without Copilot fallback or automatic lock deletion. An explicit API key or OAuth token bypasses this helper-only local-login transaction; the named direct Claude lane never inherits it.
- Use `$review-orchestration-playbook` as the only review entrypoint for named single, double, and triple review plus PR readiness. Named double adds actual Claude Code; the detailed lane contracts below govern its independent workspace and validated runtime.
- A named single review is exactly one clear/fresh-context Codex `reviewer` agent. Launch it with `fork_turns="none"` or the platform-equivalent zero inherited turns, run it read-only in its own clean detached Git workspace produced by the trusted pre-status materializer, and let it load every applicable skill, scoped `AGENTS.md`, and project-guidance file. Before launch, prove the frozen range and both endpoint trees are locally complete with lazy fetching disabled; do not let reviewer Git commands fetch missing objects or prompt for credentials. The orchestrator must identify the authoritative active playbook path/version. For a self-policy migration, candidate-head Markdown may be read as review subject and scoped guidance, but the parent-owned reviewer profile, prompt, `materialize-worktree`, `validate-worktree`, compatible-version/provenance preflight, Claude launcher, and stream validator/schema must come from an independently trusted bundle pinned outside the candidate head/range; record that bundle's absolute source path, version, and SHA-256 digest. The guard must load only the manifest-bound runtime source bytes, never expose a bundle directory to ordinary import resolution, and never consume bytecode or native extension substitutes. If no compatible trusted guard exists, bootstrap under the prior trusted policy, merge and release the candidate, and only then activate the new guard; never execute candidate-head Python or shell as review-control bootstrap. Give the reviewer only review-control metadata: the repository/worktree, frozen `base_sha..head_sha`, materialization/validation receipt, trusted-bundle identity, exact sanitized Git argv prefix, instruction-loading order, read-only/evidence limits, focus/non-goals, and output contract. The reviewer must obtain and inspect the diff itself with bounded Git/tool calls; never prebuild or inject the full diff into its prompt.
- A named double review is that single-review agent plus an actual Anthropic Claude Code process launched directly in a different independent read-only clean detached Git workspace over the same frozen `base_sha..head_sha`; the supplied-diff/private-Git `isolated_review` helper is not this lane. The direct lane uses trusted real `HOME` through the canonical local-login-only interface described below. A separately requested GitHub Copilot diagnostic never satisfies or increments the named double-review shape.
- The canonical Claude Code compatibility range is `>=2.1.211,<3.0.0`, defined once in `skills/review-orchestration-playbook/scripts/review_runtime/claude_version_policy.py`. Exact patch versions belong in signed per-version artifact or schema evidence, not in global eligibility policy; Claude Code `2.1.212` is the audited stream-schema baseline, not a global pin. Every selected version must pass publisher provenance, the mandatory credential-free `--help` capability probe, accepted parent-private preflight evidence and digest bindings, and strict stream-contract gates before it can supply findings.
- Immediately before either local named lane starts, invoke `materialize-worktree` and then `validate-worktree` through a parent-validated absolute Python interpreter with `-I -B -S`, the source-only absolute guard path in the recorded trusted bundle, and the contract's fixed clean environment against that lane's exact frozen base/head and owner-private absent destination. The materializer must isolate config, attributes, hooks, fsmonitor, filters, submodules, remotes, and object dependencies before it initializes the private repository, imports the exact hard-bounded reachable-object closure, and performs detached checkout; never use `git worktree add`, clone/fetch/upload-pack, or a pre-validator status query. The validator's forced status must be the first status operation. Never execute the guard directly, resolve Python from ambient `PATH`, load global or user site initialization, or resolve a bare repository-relative `scripts/named_lane_guard`. Any repository-visible `include.path` or `includeIf.*.path` is `blocked-safety`; evaluate other safety-relevant Git values from direct local configuration with includes disabled. Launch the exact revalidated actual Claude executable only through that same trusted interpreter and guard's process-only `run-claude` subcommand; its test-oriented timeout, stream, and prompt overrides may only retain or tighten the production caps. Load the parent-recorded trusted `$review-orchestration-playbook` source for the property-scoped materialization, symlink, interpreter, process-bound, failure, and non-guarantee contract.
  The materializer must also bind the exact source `.git` marker/admin/common/object directories and linked-worktree back-pointer, reject suffix-DWIM, shallow, alternate, and promisor sources, cap object count/logical bytes/checkout entries and bytes/compressed pack bytes, prove the private destination inventory equals the frozen base/head closure, force/persist Git-false commit-graph and multi-pack-index consumption, and run bounded full object-validity `fsck` before checkout. Treat the resolved fixed-path Git executable as a declared trust root rather than claiming the guard validates its publisher identity.
- For the Claude lane's accepted real-`HOME` native sandbox, treat outside-workspace read exclusion as prompt/model scope. Native `allowRead` is not a global host-read whitelist: launch must request global `denyWrite` and critical-sensitive-root `denyRead`; those requested controls define the native-sandbox enforcement boundary, while sandboxed Bash may technically read another host path not covered by `denyRead`. Record them as requested configuration because capability output and `system/init` evidence—including the audited Claude Code 2.1.212 baseline—do not attest the final merged sandbox or managed permission arrays.
- Capture canonical Claude raw stdout outside the model-visible worktree and require the trusted bundle's `named_lane_guard validate-claude-stream` profile, launched through the recorded absolute Python interpreter with `-I -B -S`, to return `classification: accepted` for the exact parent-private preflight result, cwd, model, fixed local-login authentication source, and child return code before any findings count. That profile raw-loads the manifest-bound validator, compatibility profile, audited schema baseline, and capability-contract evidence; binds their digests; selects the reviewed closed legacy or extended stream profile for the preflight-selected version; and validates every admitted intermediate event against the selected closed, session-bound contract before checking the terminal result. Direct execution of `validate_claude_stream.py` is compatibility-only. Prose inspection, partial output, or an ad hoc parser never satisfies the lane; validator acceptance still does not prove the merged sandbox.
- A named triple review is the named double review plus a complete terminal provider-authored GitHub Codex findings payload requested with exact `@codex review` and bound to the current PR head, whole-PR range, and isolated request. The third lane supports only exact host `github.com`; every other host, including `sqbu-github.cisco.com` and every GitHub Enterprise host, and every operating identity in `{hoteng, hoteng_cisco}` is unsupported. Only review/comment evidence from exact REST `user.login == "chatgpt-codex-connector[bot]"` with exact `user.type == "Bot"` can complete the lane: fetch the review body plus every fully paginated associated inline review comment, or a terminal issue-comment body. Missing or ambiguous payload, terminal nature, or association is `triple-inconclusive`. Exact `app.slug == "chatgpt-codex-connector"` check/run evidence is service-start evidence only and never completes triple or proves a clean/no-findings result, even when current-head, post-request, and `completed` / `success`; a same-App check may be unrelated and success may coexist with review findings. Unknown identities are inconclusive. Allow at most one acceptable exact request per unchanged head and never post a second one. Server timestamps prove ordering, not request/run lineage; review/comment evidence is triple-inconclusive whenever an older request might overlap. If the PR merge base changes after that request while the head stays unchanged, invalidate the old whole-PR evidence but do not post a replacement same-head request; report readiness `blocked-input` (`base-changed-same-head`) and effective `triple-inconclusive` until a separately authorized ordinary change creates a new head. Never manufacture an empty or anchor commit merely to regain GitHub-lane eligibility.
- Resolve the local range and PR selector independently: preserve an explicit frozen range for local lanes, while an explicitly named PR or exactly one authenticated open-PR candidate for the exact current head repository/branch selects PR-specific/triple work. Explicit-range-only single/double requires no PR probe. Multiple required PR candidates leave the GitHub/PR-specific lane `blocked-input` until the caller names a PR; a frozen range does not select among them, although fully scoped local lanes may run. For every selected PR, independently read `baseRefName`, `baseRefOid`, and `headRefOid`; with lazy fetching disabled, require both endpoint commits locally and require `git merge-base --all pr_base_oid pr_head_oid` to produce exactly one `pr_merge_base`. A selected PR's explicit frozen range satisfies PR-specific readiness or triple completion only when `base_sha == pr_merge_base` and `head_sha == pr_head_oid`. A same-head/different-base range is `blocked-input` (`scope-mismatch`): preserve the caller's range, do not silently rewrite it, and never describe its local review results as whole-PR coverage. Explicit-range-only standalone single/double with no selected PR is unaffected. A proven no-PR result does not supply a range; require an explicit committed range or explicitly named target/base and never guess it. For triple, detached HEAD or unknown head ownership without an explicit PR is `blocked-input` / `triple-inconclusive`, not effective double; the fully scoped local lanes may still run. A bare named-review request is report-only and does not authorize branch creation, an anchor commit, push, PR creation, or PR branch/metadata changes. Reserve `blocked-authorization` for intended dirty/untracked state that would require an unauthorized anchor commit.
- Every selected PR, including one named explicitly, must also report exact lifecycle `state == "open"`, `merged == false`, and `merged_at == null` before local PR-specific lanes or third-lane work. Missing/contradictory lifecycle evidence is `blocked-input` (`pr-lifecycle-unverified`) and triple-inconclusive; closed-unmerged is `selected-pr-closed` and may reduce only an otherwise fully scoped, never-started third lane to effective double; merged is terminal `already-merged` / `selected-pr-merged`. Revalidate lifecycle before posting `@codex review`, before accepting its result, and before readiness/merge. An observed non-open lifecycle at any mandated snapshot after request/service start invalidates evidence and remains triple-inconclusive. These point-in-time snapshots do not prove that no intermediate close-and-reopen occurred between them.
- For each selected PR's first range freeze, persist immutable parent-owned `range_origin.kind`, `range_origin.base_sha`, and `range_origin.head_sha`; the kind is exactly `caller-supplied` or `pr-derived`, and original caller endpoints are never overwritten. On a post-request base-only retarget, apply the playbook's machine-readable state contract before generic `scope-mismatch`: stale, unknown-origin, or unauthorized parent-rewrite state stops before local lanes; a caller-supplied exact-current recovery or valid PR-derived exact-current rederivation may rerun the local lanes, but the same-head GitHub lane remains `triple-inconclusive` and no second request is allowed.
- Bare triple authorizes only the scoped review-request comment on an already-existing supported PR. Every selected existing PR still requires intended-head/current-head alignment before local lanes or readiness-state consumption, even when an unsupported host or identity has already reduced triple to effective double. Explicit-range-only standalone single/double has no selected PR and requires no comparison; authenticated actual PR absence likewise has none. If a still-eligible PR's current `headRefOid` does not equal the frozen `head_sha` and mutation is not separately authorized, leave it unchanged and report requested triple/effective triple-inconclusive with GitHub lane status `blocked-authorization`. For the same mismatch on an already unsupported PR, keep `requested: triple`, `effective: double`, and report readiness `blocked-authorization`; do not treat the mismatch as making the already-unavailable lane triple-inconclusive or as permitting readiness to continue. If GitHub Codex is unavailable because there is no PR or the integration/host/identity is unsupported, report `effective double`; never claim triple. An authenticated rejection from the exact accepted provider identity may prove that no run started and the integration/service is unavailable; missing response or generic failure is inconclusive. After exact-identity acknowledgement/run/review activity proves start, malformed/stale/ambiguous/incomplete evidence is `triple-inconclusive`, not double fallback.
- The legacy `isolated_review` Codex helper, if retained for low-level compatibility or diagnostics, does not count toward single, double, or triple review; never count a supplied-diff helper as a named lane. PR readiness adds CI, conversation, and branch/base gates, but no retired extra Codex gates.
- The low-level `isolated_review` helper retains its helper-private credential staging, lock, guarded-writeback, and recovery contracts in `helper-contract.md` and `claude-runtime-trust.md`. Those guarantees do not apply to the named direct Claude lane: its only active authentication interface is ordinary local login in trusted real `HOME`, including credential refresh and possible cache or tool-result artifacts. The direct lane accepts no API-key or OAuth-token environment interface; validator/schema recognition of `ANTHROPIC_API_KEY` is compatibility evidence only and does not make that launch mode available. When refresh is forbidden or local login is unavailable, report `blocked-authentication`. Do not claim helper broker/carrier guarantees or that `--no-session-persistence` makes real `HOME` immutable.
- Any unambiguous request classified as single, double, or triple review—including `single review`, `single code review`, `单重 review`, `单一 review`, `double`, `double review`, `double code review`, `双重 review`, `triple`, `triple review`, or `三重 review`—is contemporaneous user consent for scoped code-review egress to exactly that named shape. Single authorizes OpenAI Codex; double additionally authorizes Anthropic Claude Code; triple additionally authorizes GitHub Codex on an exact-host `github.com` PR. The selected reviewer is a trusted processor and may inspect the named repository's original tracked diff, necessary tracked context, bounded tool-derived evidence, and review prompt/result, including tracked repository secrets. The Codex `reviewer` agent receives the clean worktree and refs rather than an injected full diff; actual Claude Code reviews the same range from its independent read-only workspace. Do not redact, rewrite, or suppress tracked reviewer input because of a secret-delta result. This consent does not authorize GitHub Copilot or another substitute reviewer, automatic discovery of reviewer/runtime authentication credentials, untracked files, unrelated repositories, or broad workspace/home-directory content. For an approval-gated Claude invocation, repeat this consent together with the exact repository and `base_sha..head_sha` in the escalation justification; do not rely on a generic external-review justification.
- Secret-delta analysis never blocks a named reviewer launch; it is an independent PR/master admission control. Count each exact raw secret byte value globally over the complete base and head tracked trees, including raw Git path bytes, regular-file blobs, and symlink-target bytes, and allow `head_count <= base_count`. Only a first appearance or count growth blocks admission; unchanged values, deletions, and moves pass. Do not derive Base64, hex, URL-encoded, or other transformed variants; this is an explicit detection limitation. A genuinely incomplete bounded scan is `inconclusive`, and violation output lists only head-side added locations, not unchanged occurrences.
- PR/master exact-secret admission uses `isolated_review secret-admission --repo <repo> --base-ref <base_sha> --head-ref <head_sha>`. This direct Git-tree scan has `review_contract: admission-only-no-reviewer`, does not materialize a workspace, diff, or prompt, and never starts a reviewer. Only exit `0` (`clean`) with `temporary_cleanup_status: complete` permits admission; exit `1` means proved violations and remains `1` even if later location mapping or temporary cleanup is incomplete, while exit `75` means an inconclusive scan or a clean scan whose temporary cleanup failed. A head change invalidates the result. The retained `stateful final` / `stateful admission` pair belongs only to an explicitly chosen low-level helper run and is never a required PR-readiness producer.
- Use `$remote-host-context` when task evidence may span Joey's local machine or any host in that skill's default evidence scope, especially for work-report audits, session/history scans, or recent-activity recovery that would be skewed by local-only evidence.
- For workflows rooted in `~/.codex`, `~/.codex/rules`, `~/.cursor/cli-config.json`, Apple Notes, downloaded local archives, or other host-level machine state, prefer a personal skill in `~/.codex/skills` over a repo-local mirror unless the workflow genuinely depends on repo-specific scripts or policy.
- Unless a repo-local `AGENTS.md`, repo-local skill, or stronger project convention says otherwise, use `$project-journal` as the default Joey repo tracking workflow: write ordinary PR/thread/workstream state to `docs/project_journal/YYYY/MM/*.md`, keep `docs/PROJECT_STATE.md` and `docs/PROJECT_TODO.md` as stable short entrypoints for repo-wide status and cross-workstream backlog only, treat generated `docs/project_journal/INDEX.md` as a local ignored artifact, and avoid maintaining giant tracked status/TODO files.
- For PR-bound project journal updates, first check the repo's merge model. In squash-merge repos, write tracked journal docs as the target-branch post-merge state and prefer updating the relevant per-workstream journal over the top-level entrypoints: if the PR fully completes the workstream, use `status: completed` before merge and keep transient states such as "waiting for merge", "not merged yet", or "ready for review" in the PR body, checklist, or comments. If the merge model is unclear or not squash-merge-only, keep each commit's docs self-consistent and do not mark work completed before the same commit contains the completed implementation.
- Use `$bug-triage-playbook` for log-driven debugging, regression analysis, flaky behavior, or root-cause work that starts from symptoms.
- For repeated Jenkins or remote archive triage, keep auth/fetch preflight direct and use the `bug-triage-playbook` helper script/reference for the repetitive local archive-inspection steps instead of rebuilding the same shell wrapper chain from scratch.
- When the user provides a specific remote log, archive, crash report, or build URL, treat it as the primary evidence. Verify access and auth first; do not quietly fall back to stale local artifacts.
- When Cursor Agent, a legacy review CLI, or a remote artifact fetch may need approval, network, or auth, do a narrow preflight first and report the exact missing gate before attempting the full workflow.
- When an approved command family likely already exists, prefer invoking the real executable directly instead of wrapping it in `bash -lc` or `/bin/zsh -lc`. Use a shell wrapper only when pipes, redirection, heredocs, or inline env assignment are actually needed.
- When a review or debugging pass needs adjacent context beyond the explicitly scoped files, verify the candidate path first with `rg --files`, `git diff --name-only`, or a similarly bounded listing before opening it; do not guess sibling filenames and probe them with `sed`/`cat`.
- Execute single-command instructions directly, even when they need escalation, instead of writing a script file first. When a task can be cleanly split into a few direct commands, run those commands one by one instead of wrapping them in a multi-step script. Reserve HERE DOC for short read-only probes that stay inside the current sandbox and do not need approval. For non-trivial multi-line shell or Python logic, do not inline it via HERE DOC or `python -c`; prefer writing a task-scoped script file and executing it instead. Before requesting approval for that script, briefly state what it will do.
- When passing URLs or other arguments that contain shell metacharacters such as `*`, `?`, `[`, `]`, `&`, backticks, `$`, or newlines, prefer direct argv forms; if a shell wrapper is unavoidable, quote those arguments explicitly so the shell cannot rewrite them before the helper sees them. For GitHub PR/issue bodies or other Markdown payloads, write a task-scoped file and use tool-native file input such as `gh pr create --body-file` instead of inline shell `--body`.
- In sandboxed or approval-gated sessions, if `git rev-parse --git-dir` and `--git-common-dir` differ or point outside the cwd repo, treat it as a linked-worktree/shared-gitdir write path. Not only `git switch`, `git worktree add`, and `git submodule update --init`, but also `merge`, `stash`, `restore`, `cherry-pick`, branch/ref updates, and similar writes can fail on `.git/worktrees/.../*.lock`, `.git/index.lock`, `.git/sequencer`, or `.git/config`; preflight that split first and move to a writable source-repo context or narrow approval instead of repeating the same doomed git write.
- For cleanup inside a repo's `.codex-tmp`, prefer `~/.codex/bin/codex-clean-tmp <subpath...>` or `~/.codex/bin/codex-clean-tmp --all` instead of raw `rm -rf`; treat that helper as protected infrastructure and do not modify it unless Joey explicitly asks.
- For recurring scheduled/background chores, prefer Codex Automations paired with skills; use Rules to control sandbox escapes instead of treating `AGENTS.md` as a pseudo-hook system.
- Before creating a dependency-wait automation, verify that all required terminal conditions can hold simultaneously and account for superseded or alternative completion paths plus external minimum versions; if the conditions conflict or remain undecided, do not encode a permanent AND wait.
- For `lsof`, prefer the normalized `-nP` forms such as `lsof -nP -p <pid>` or `lsof -nP -iTCP` so diagnostics and recurring approvals do not drift across `-n` / `-nP` variants.
- When creating substantial temporary artifacts (for example under `/tmp` or `.codex-tmp`), prefer task-scoped directories, clean them up before finishing when safe, and explicitly report any leftovers you intentionally keep.
- If helper validation scripts fail because local dependencies are missing, do a lightweight fallback validation and state exactly what was checked.
- If a repo-local `AGENTS.md` has unrelated user edits, mention it once and continue unless it directly conflicts with the current task.
- When an external review path or live validation gate stays inconclusive after bounded retries, stop at a crisp decision point with evidence instead of looping indefinitely.
- For any long-running command you may need to poll, interrupt, or harvest final output from, start it with a pollable TTY/PTY shape; do not assume `write_stdin` can attach to a plain-pipe session after stdin closes. For external review helpers, if the subprocess is still alive but output goes dark, change the session shape before launching a second reviewer.
- When polling with `wait_agent`, omit `timeout_ms` to use the `30000` millisecond default or keep it within the supported `10000`–`3600000` millisecond range. Prefer `10000` when a response is imminent and `30000`–`60000` for ordinary or reviewer polling; longer single waits are valid but usually weaken user-facing progress updates.
- Prefer `gh` CLI for GitHub-specific interactions.
- For debugging or review tasks, lead with the decisive evidence or findings rather than narrating the whole search.
- When only key evidence matters, quote or summarize the most relevant 5-10 log lines and label them as key lines instead of pasting whole logs.
- When build or tooling details matter, include the relevant tool path and version to reduce follow-up questions.
- For multi-line commit messages, use `git commit -F` or multiple `-m` flags instead of embedding literal `\n`.
- Do not suggest history-rewriting Git commands such as `git rebase`, `git reset --hard`, or `git push --force` unless explicitly requested.
- For commits I create, use `-S`; include a `Co-authored-by: Codex ... <codex@openai.com>` footer when policy requires it.
- When a signing operation fails with `No pinentry`, stop trying alternate shell, TTY, agent, or command shapes. Run one narrow pinentry/TTY/agent-access preflight; if it shows a locked key, ask Joey to unlock it, otherwise report the specific configuration or sandbox blocker, then retry the original Git command at most once after resolution.
- For PRs I create, if every commit in the PR was authored by Codex, prepend the PR body with the note block below. Keep the model version current as the default Codex model changes; `high` and `xhigh` reasoning labels can be treated as interchangeable unless the actual run materially differs.

  > [!NOTE]
  > This PR is purely generated by LLM: OpenAI Codex (GPT-5.6 Sol (reasoning xhigh, summaries auto)).
- In this environment, sandboxed GPG signature verification may fail to reach `~/.gnupg` / `keyboxd`; when signature status matters, rerun a narrow verification command with escalation before concluding the signature is bad.
- In personal skills, name important platforms or product variants explicitly in the frontmatter when they affect triggering.
